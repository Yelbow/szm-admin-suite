# SPEC

> Wensen van de gebruiker, letterlijk — geen geïnterpreteerde of geparafraseerde requirements. Dingen hier toevoegen gebeurt pas nadat de gebruiker het zelf gezegd heeft.

- "a plugin that has multiple different functions inside" — modules die je aan/uit kunt zetten.
- "i dont know how this works and if this compares with something. but like modules maybe you can turn off and on."
- Module 1: "a plugin/module/function that allows to chage the wp-admin theme and appearence and visually change it a lot if possible."
- Module 2: "make really good and interesting dashboard and maybe recommandation for plugins to install so we have intersting stuff to show for my clients."
- "and also be default have dumb screen elements off."
- "prepare this project. then /grill-me then write specs and plans."
- Naam: **SZM Admin Suite** (folder `szm-admin-suite`), naast de bestaande menu-manager in `/Dev/`.
- Modules v1: Admin theme/appearance, Custom dashboard, Declutter, White-label/branding. Menu-restrictie komt later over uit de bestaande plugin.
- Menu-restrictie: "want to put the menu restriction from my current plugin in there at some point" → LATER, pas nadat v1 bewezen is; bestaande plugin blijft voorlopig.
- Admin theme module: "this is mostly i place i can put themes in. so i tell ai to build a cyberpunk theme and he puts a theme there. then we start working on a pink kawaii theme. seperatly. then i can switch between them and also continue working on them with ai individuelly." → drop-in theme folders.
- Dashboard: "no basic shit actual usefull stuff. visitors stats etc, Plugin recommendations card, Welcome card." Maar visitors stats: "No stats in v1."
- "only the default site health can stay" — op het custom dashboard mag alleen de Site Health widget van de defaults blijven.
- Theme vs white-label grens: "Yes, that split" → theme module = kleur/typografie/layout/presets; white-label = logo, footer tekst, login achtergrond.
